// Exports `Triangle`, `Circle`, and `Square` classes

// create a shape class constructor that takes in height, width, color, and textcolor

class Shape {
    constructor(height, width, color, textcolor) {
        this.height = height;
        this.width = width;
        this.color = color;
        this.textcolor = textcolor;
        
    }
}

// create a circle class that extends the shape class
    

// create a square class that extends the shape class

// create a triangle class that extends the shape class




// export the shape class
module.exports = Shape;